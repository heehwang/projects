public class BinarySearchTree extends BinaryTree {

	public boolean contains(Comparable key) {
		return containsHelper(key, this);
	}

	public boolean containsHelper(Comparable key, BinarySearchTree t) {
		if (myRoot == null) {
			return false;
		} else if (key.compareTo(t.myRoot.myItem) == 0) {
			return true;
		} else if (key.compareTo(t.myRoot.myItem) < 0) {
			return containsHelper(key, t.myRoot.myLeft);
		} else {
			return containsHelper(key, t.myRoot.myRight);
		}
	}

	public void add() {

	}

}